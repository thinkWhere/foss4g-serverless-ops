[
  {
    "name": "geoserver-service",
    "image": "${repo_url}:latest",
    "portMappings": [
      {
        "containerPort": 8080,
        "protocol": "tcp"
      }
    ],
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${cloud_watch_group}",
        "awslogs-region": "${region}",
        "awslogs-stream-prefix": "awslogs-geoserver-prefix"
      }
    },
    "essential": true
  }
]
